package tech.claudioed.shipment.domain;

import io.quarkus.test.junit.SubstrateTest;

@SubstrateTest
public class NativeresourcesIT extends resourcesTest {

    // Execute the same tests but in native mode.
}