package tech.claudioed.shipment.domain;

import io.quarkus.test.junit.SubstrateTest;

@SubstrateTest
public class NativeresourcesIT extends shipmentResourcesTest {

    // Execute the same tests but in native mode.
}